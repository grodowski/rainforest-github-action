on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Test & Deploy
    steps:
      - uses: actions/checkout@master
      - name: RF LOL
        uses: rainforestapp/rainforest-github-action@master
        with:
          rainforest_api_key: ${{ secrets.RAINFOREST_API_TOKEN }}